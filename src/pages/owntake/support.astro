import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title="OwnTake Support"
      description="Support information for OwnTake."
    />
  </head>
  <body>
    <Header />
    <main>
      <section class="hero support-hero">
        <div class="hero-box">
          <p class="kicker">OwnTake · Short-form video structure</p>
          <h1 class="hero-title">Make the next version yours.</h1>
          <p class="lead">
            Need help importing footage, analyzing a format, managing a subscription, or exporting
            a remix? We’re here to help.
          </p>
          <div class="hero-actions">
            <a class="button" href="mailto:randyventures06@gmail.com">Email support</a>
            <a class="button secondary" href={import.meta.env.BASE_URL + '/owntake/privacy'}>
              Privacy policy
            </a>
          </div>
        </div>
      </section>

      <div class="support-grid">
        <section class="tile tile-accent">
          <p class="tile-kicker">Contact</p>
          <h2>Reach Randall Ventures.</h2>
          <p>
            Email <a href="mailto:randyventures06@gmail.com">randyventures06@gmail.com</a> and
            include your device model, iOS version, and a short description of what happened.
          </p>
        </section>

        <section class="tile">
          <p class="tile-kicker">Helpful details</p>
          <h2>Help us reproduce it.</h2>
          <ul>
            <li>Which screen or workflow you were using</li>
            <li>The type and duration of the file, if importing</li>
            <li>Any error message shown by the app</li>
            <li>A screenshot or short screen recording, when helpful</li>
          </ul>
        </section>
      </div>

      <p class="legal-links">
        <a href={import.meta.env.BASE_URL + '/owntake/privacy'}>Privacy Policy</a>
        ·
        <a href={import.meta.env.BASE_URL + '/owntake/terms'}>Terms of Use</a>
      </p>
    </main>
    <Footer />
  </body>
</html>

<style>
  .support-hero {
    padding-top: 0;
  }

  .support-hero .hero-box {
    background: var(--accent-tile);
    border-color: transparent;
    box-shadow: var(--shadow-1);
  }

  .support-hero .kicker,
  .support-hero .hero-title {
    color: var(--on-accent);
  }

  .support-hero .lead {
    color: color-mix(in srgb, var(--on-accent) 76%, transparent);
    max-width: 42rem;
  }

  .support-hero .button {
    background: var(--on-accent);
    color: var(--accent-tile);
  }

  .support-hero .button.secondary {
    background: transparent;
    color: var(--on-accent);
    border-color: color-mix(in srgb, var(--on-accent) 35%, transparent);
  }

  .support-grid {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 14px;
  }

  .support-grid h2 {
    font-size: 1.45rem;
  }

  .support-grid p,
  .support-grid li {
    color: color-mix(in srgb, var(--on-accent) 76%, transparent);
  }

  .support-grid > .tile:not(.tile-accent) p,
  .support-grid > .tile:not(.tile-accent) li {
    color: var(--ink-muted);
  }

  .support-grid ul {
    margin: 0;
    padding-left: 1.25rem;
  }

  .support-grid li + li {
    margin-top: 0.55rem;
  }

  .legal-links {
    margin-top: 1.25rem;
    text-align: center;
    color: var(--ink-muted);
  }

  @media (max-width: 760px) {
    .support-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
